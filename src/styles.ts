import { StyleSheet } from "react-native";
import { COLORS } from "./constants/colors";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  contentCard: {
    flex: 1,
    backgroundColor: COLORS.colorContenCards,
    padding:20,
  },
});
